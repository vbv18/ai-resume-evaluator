[project]
name = "server"
version = "0.1.0"
description = "FastAPI backend for AI Resume Evaluator."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.139.2",
    "groq>=1.5.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.51.0",
]
